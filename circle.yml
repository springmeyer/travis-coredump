machine:
  environment:
    CRASH_PLEASE: boooooooom

checkout:
  post:
    - gcc --version

## Customize dependencies
dependencies:
  pre:
    - sudo apt-get -y update && sudo apt-get -y install gdb apport

  cache_directories:
    - "./mason_packages"

test:
  override:
    - ./ci-build.sh